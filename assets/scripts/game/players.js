const createPlayers = function () {
  const playerA = createPlayer ('Red Team')
  const playerB = createPlayer ('Blue Team')
}

const createPlayer = function (team) {
  const teamName = team
}


module.exports = {
  createPlayers,
}
